{
  "name": "orderbook-stripe-server",
  "version": "1.0.0",
  "description": "Minimal Express server to create Stripe Checkout sessions for local development",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "stripe": "^12.0.0"
  }
}
